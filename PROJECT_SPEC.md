# Project Contract

This repository contains a CPU-only hybrid Python/C++ training pipeline. Students repair a small number of explicit faults, merge required course branches, cherry-pick a deterministic dataset patch, revert a known-bad training configuration commit, and produce a reproducible checkpoint.

Public checks are feedback only. Authoritative grading must run from a trusted private grader.
